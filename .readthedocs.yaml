version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"  # Use a stable version

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - path: .  # Install your package: spacecurves
